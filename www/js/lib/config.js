var DEFAULT_FILE_NAME = require('../../../config.json');

var config = {
    openweather_api: DEFAULT_FILE_NAME.openweather_api,
    openweather_api_key: DEFAULT_FILE_NAME.openweather_api_key
};

module.exports = config;
